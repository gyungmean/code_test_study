import java.util.*;
class Solution {
    public String[] solution(String[] strings, int n) {
        String[] answer = new String[strings.size()];
        Arrays.sort(strings);
        String[] = new String[strings.size()];
        
        for(int i = 0; i < strings.size(); i++){
            s.charAt(i).subString;
        }
        return answer;
    }
}
